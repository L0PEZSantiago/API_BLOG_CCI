include:
    - UserFixtures.yaml

App\Entity\Article:
    article_{1..12}:
        title: Article <current()> # Le current permet de numéroter les articles en fonction de leur index
        content: This is the content of article <current()>.
        shortContent: This is a short content for article <current()>.
        enabled: <boolean()>
        user: <randomElement([@user_admin, @user_user])> # On assigne un utilisateur au hasard
